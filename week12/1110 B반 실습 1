#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void getInputValue(char str[][100]) {
    for (int i = 0; i < 6; i++)
    {
        scanf("%s", str[i]);
    }
}

void printValue(char str[][100]) {
    for (int i = 0; i < 6; i++)
    {
        printf("%s\n", str[i]);
    }
}

int main() {

    char str[6][100];

    getInputValue(str);

    printValue(str);

    return 0;
}
